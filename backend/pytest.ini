[pytest]
DJANGO_SETTINGS_MODULE = lifemanager.settings
python_files = tests.py test_*.py *_tests.py
addopts = 
    --strict-markers
    --tb=short
    --cov=apps
    --cov-report=html
    --cov-report=term-missing
testpaths = tests
